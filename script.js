function shortcut(s1, s2) {
  // your code here
	if(s1==string && s2==string){
			return s1[0] + s2[0];
	}
}

// Do not change the code below.
const s1 = ("Enter s1:");
const s2 = ("Enter s2:");
(shortcut(s1, s2));
